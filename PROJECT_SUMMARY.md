# Yomii 项目总结

## 1. 项目目标

Yomii 是一个面向日语学习的课程项目，核心目标是把查词、背单词、测试和作文评测四条学习链路整合到同一个系统中。

当前项目已经形成完整的前后端闭环：

- 前端：Vue 负责页面交互、学习流程和结果展示
- 后端：FastAPI 负责用户、词库、学习记录、测试记录、作文记录和统一编排
- 数据层：SQLite 存词典，MySQL 存用户行为和历史记录

## 2. 已完成的核心模块

### 查词

- 支持词汇查询
- 支持收藏
- 支持搜索历史持久化
- 搜索历史从纯前端逻辑改成后端存储

### 背单词

- 学习计划与词典等级联动
- 学习状态分为掌握、模糊、不认识
- 支持加量学习、复习、完成态统计
- 前后端词级和 `word_tags` 已对齐

### 测试

- 测试题目与后端词库和标签对应
- 支持测试历史记录
- 支持能力报告生成
- 报告计算考虑历史记录、时间衰减和难度层级

### 作文评测

- 前端提交作文
- 后端异步生成作文评测任务
- 支持评分报告、修改建议、修正版和历史记录
- 支持 mock 模式和外部模型服务接入

## 3. 作文 AI 的训练路线

作文模块采用了双模型思路，但训练时共享同一个基础模型：

- 基础模型：`Qwen3-1.7B`
- 训练方式：`QLoRA 4-bit`
- 训练目标：
  - `score-lora`：负责 JLPT 风格评分
  - `revision-lora`：负责作文修改建议与修正版生成

训练代码保留在：

- [backend/training](D:\MyCode\软件开发项目\yomii\backend\training)

训练阶段的关键思路：

1. 用公开语料和整理后的样本构造评分/修改训练集
2. 在 `Qwen3-1.7B` 上分别训练两个 LoRA adapter
3. 训练完成后保留 adapter 产物和训练代码
4. 清理训练数据、缓存和临时环境，避免项目体积失控

## 4. 作文 AI 的接入方式

后端不直接绑定某一种本地模型部署方案，而是通过统一接口编排作文评测：

- 评分接口：`ESSAY_SCORE_MODEL_URL`
- 修改接口：`ESSAY_REVISION_MODEL_URL`

当前仓库支持三种接法：

1. `默认本地模型` 模式  
启动评分模型和修改模型服务，后端通过本地 URL 统一编排调用。

2. `现成模型服务` 模式  
把 URL 指向已有的模型服务，后端按统一协议调用。

3. `mock` 模式  
模型 URL 留空，后端返回内置 mock 结果，适合联调和兜底演示。

## 5. 当前仓库状态

为了控制空间，项目已经做过一次清理：

- 已删除训练阶段缓存
- 已删除临时下载目录
- 已删除虚拟环境目录
- 已删除训练数据中间产物
- 保留了业务代码、训练代码、模型产物和必要数据文件

当前更适合作为课程项目交付版本，而不是完整模型训练工作区。

## 6. 运行方式

当前推荐运行方式：

1. 启动前端
2. 启动后端
3. 作文模块默认用 mock 或接已有模型服务

不再要求仓库内强制附带本地模型部署环境。

## 7. 项目价值

这个项目的价值不只是页面展示，而是把下面这些能力真正接到一起：

- 词典数据和学习数据分离存储
- 学习计划、复习、测试、作文统一进入用户成长链路
- 测试能力报告与作文报告都支持历史记录
- AI 能力不是孤立页面，而是作为后端服务被系统编排

从课程作业角度，这已经是一个完整、可解释、可运行的日语学习系统原型。
